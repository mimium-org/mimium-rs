// @test {"times":1,"stereo":true,"expected":[8.0, 8.0],"web":true}
// `|>` and `||>` share precedence and associate to the left.
fn dsp(){
    let x = 3.0 ||> _ + 1.0 |> |arg| arg * 2.0
    let y = 3.0 |> |arg| arg + 1.0 ||> _ * 2.0
    (x, y)
}