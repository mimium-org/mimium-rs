// Test multiple imports: use mod::{a, b}

mod math {
    pub fn double(x) { x * 2.0 }
    pub fn triple(x) { x * 3.0 }
    fn private_fn(x) { x }  // Should not be accessible
}

use math::{double, triple}

fn dsp() {
    double(10.0) + triple(10.0)
}
