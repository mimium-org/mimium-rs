${

let genpower = |n:float| -> `(float)->float {
    letrec aux = |n1:float,x| {
        if (n1>0){
           `{$( aux(n1-1,x)) * $x }
        }else{
           `1
        }
    }
    `{|x:float| $(aux(n,`x)) }
}

`{ 
    let dsp = | | 2 |> $(genpower(5)) 
}
}