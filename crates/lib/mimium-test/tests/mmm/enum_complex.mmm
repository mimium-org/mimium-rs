type MyEnum = One(float) 
            | Two(float)

fn test(myenum: MyEnum){
  match myenum {
    One(v) => v*1,
    Two(v) => v*2
  }
}

let x = test(One(4))
let y = test(Two(5))

fn dsp(){
    x+y
}
