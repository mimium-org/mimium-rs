#stage(macro)
fn test_lift_arrayf(){
   let arr = [1.0, 2.0, 3.0, 4.0]
   arr |> lift_arrayf
}
#stage(main)
fn dsp(){
  let lifted_arr = test_lift_arrayf!()
  lifted_arr[0] + lifted_arr[1] + lifted_arr[2] + lifted_arr[3] // answer should be 10.0
}
