This directory stores test-generated source and WASM artifacts for `mimium-lang`.

Files here are written by unit tests via `repo_test_artifact_path()` so they live under a version-controlled path instead of the repository-level ignored `tmp` directory.

The contents may be regenerated by tests and are mainly kept for inspection and debugging.
