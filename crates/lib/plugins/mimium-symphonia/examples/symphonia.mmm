// Sampler_mono! returns { player, length }
let sampler = Sampler_mono!("../tests/assets/konnichiwa.aif")
fn counter(){
    self+1.0 
}
//add -1.0 offset so that the counter start from 0 at t=0
fn dsp(){
    let player = sampler.player
    player(counter()-1.0) |> ProbeValue!("out")
}