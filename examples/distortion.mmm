use drive::*
use osc::*
use env::adsr
fn dsp(){
    let src = sinwave({freq=440})
    let gain = phasor({freq=1})>0.5 |> |gate| adsr({gate=gate}) 
    src * gain 
    |> |x| bitcrush(x,2)
    |> Probe!("out")
}